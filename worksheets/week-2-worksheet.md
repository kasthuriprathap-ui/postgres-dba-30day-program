# Week 2 Worksheet — Core Administration

Full answers to Day 14's drill go here.

## A. Backup / restore (Day 8)

1. Pick the right tool for each:  
   a. Copy schema `reporting` from prod to dev → …  
   b. Rebuild a 400 GB cluster in a new region, RPO ~5 min → …  
   c. Recover one table dropped 40 min ago → …  
   d. Move a database across majors (14→16) → …  

2. Write the `pg_dump` command to dump only DDL of DB `shop`, schema `sales`, for restore via `psql`.  
   _Answer:_ …

3. Design a `cron` (or scheduler) for daily `pg_dump -Fc` of `shop` at 03:00 UTC, 14-day disk retention, ship newest to object storage.  
   _Answer:_ …

## B. WAL & PITR (Day 9)

4. Minimum parameters to enable PITR on a self-hosted cluster.  
   _Answer:_ …

5. Sketch a WAL archiving pipeline for 30 clusters. Where does `pgBackRest`/`WAL-G` fit?  
   _Answer:_ …

6. `pg_stat_archiver.failed_count = 812`. Walk your triage in five bullets.  
   _Answer:_ …

## C. Vacuum & bloat (Day 10)

7. Given `n_dead_tup = 12M`, `n_live_tup = 3M`, `last_autovacuum` = 4h ago — top three causes?  
   _Answer:_ …

8. Per-table autovacuum overrides for a very hot `events` table (50M rows, 100K updates/hour).  
   _Answer:_ …

9. On RDS, two CloudWatch metrics that are early warnings for autovacuum starvation.  
   _Answer:_ …

## D. Indexes (Day 11)

10. Pick an index type:  
    a. `WHERE lower(email) = ?` → …  
    b. `WHERE tags @> ARRAY['discount']` → …  
    c. `WHERE ts >= now() - '1 day'` on 5 TB append-only → …  
    d. Full-text French → …  
    e. Overlapping time-range exclusion → …  

11. Rewrite this SQL Server nonclustered filtered/covering index in PG:  
    ```sql
    CREATE NONCLUSTERED INDEX ix_orders_status_customer
    ON dbo.Orders (Status, CustomerId) INCLUDE (Total, OrderedAtUTC)
    WHERE Status = 'open';
    ```  
    _Answer:_ …

## E. EXPLAIN & plans (Day 12)

12. Given a plan where the outer `Seq Scan on big` estimates 1 row but returns 250,000 — what's wrong and how do you fix it?  
    _Answer:_ …

13. `auto_explain` parameter block for logging queries over 250 ms with plans and buffers.  
    _Answer:_ …

## F. Parameters & pooling (Day 13)

14. Starting values for `shared_buffers`, `work_mem`, `max_connections`, PgBouncer? (yes/no) — for:  
    a. 4 vCPU / 16 GB / OLTP, 300 app connections → …  
    b. 16 vCPU / 128 GB / mixed + occasional 20-way parallel → …  
    c. 2 vCPU / 8 GB / dev/staging → …  

15. Three parameters you cannot change on RDS. Why?  
    _Answer:_ …

## Self-scoring

**Total: ___/15.** Ready for Week 3 at 12+.
